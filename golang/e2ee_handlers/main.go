package main

import (
	"fmt"
	"net/http"
)

// Websocket - EKS
// Deployed behind AWS API Gateway with Websocket support

func connectHandler(w http.ResponseWriter, r *http.Request) {
	// Save connection id to R


func main() {

	http.HandleFunc("/ws", server.connnect)

}
